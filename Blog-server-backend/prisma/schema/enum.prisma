enum ActiveStatus {
    ACTIVE
    INACTIVE
}

enum Role {
    ADMIN
    USER
    AUTHOR
}

enum PostStatus {
    DRAFT
    PUBLISHED
    ARCHIVED
}

enum CommentStatus {
    APPROVED
    REJECTED
}

enum SubscriptionStatus {
    ACTIVE
    CANCELED
    EXPIRED
}
